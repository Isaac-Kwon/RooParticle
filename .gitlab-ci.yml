before_script:
 - yum update -y
 - yum install -y cmake3

defaultci:
    only:
        - develop
    script:
        - mkdir build
        - (cd build && cmake3 ../ && make -j $(nproc) all format-check)
