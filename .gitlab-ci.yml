image: gitlab-registry.cern.ch/mkwon/rooparticle:latest


defaultci:
    only:
        - develop
    script:
        - mkdir build
        - (cd build && cmake ../ && make -j $(nproc) all format-check)
