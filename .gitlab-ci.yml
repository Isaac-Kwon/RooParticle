image: rootproject/root-ubuntu16:latest

before_script:
 - apt-get update --yes
 - apt-get install --yes cmake3


defaultci:
    only:
        - develop
    script:
        - mkdir build
        - (cd build && cmake3 ../ && make -j $(nproc) all format-check)
